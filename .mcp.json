{
  "mcpServers": {
    "slack-channel": {
      "command": "bash",
      "args": ["-c", "exec bun run --cwd ${CLAUDE_PLUGIN_ROOT} --shell=bun --silent start 2> >(tee -a /state/inbox/server.err >&2)"]
    }
  }
}
